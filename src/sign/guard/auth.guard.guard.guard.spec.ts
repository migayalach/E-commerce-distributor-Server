import { AuthGuardGuardGuard } from './auth.guard.guard.guard';

describe('AuthGuardGuardGuard', () => {
  it('should be defined', () => {
    expect(new AuthGuardGuardGuard()).toBeDefined();
  });
});

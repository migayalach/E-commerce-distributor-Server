import { SubscribreMiddleware } from './subscribre.middleware';

describe('SubscribreMiddleware', () => {
  it('should be defined', () => {
    expect(new SubscribreMiddleware()).toBeDefined();
  });
});
